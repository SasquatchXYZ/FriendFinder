// Holds the Array 'friends' which holds the objects for each participant in the survey,
// it is also from this data that the compatibility checks are performed in order to provide your 'match'.

const friends = [];

module.exports = friends;
